/**
 * AI-BOS Cache Invalidation
 *
 * Cache invalidation utilities.
 */

export const CacheInvalidation = {
  invalidate: () => {},
  invalidatePattern: () => {},
  invalidateAll: () => {}
} as const;
