// ==================== SHELL COMPONENT EXPORTS ====================

// Main Shell Components
// Shell component not available yet

// App Container System
export { default as AppContainer } from './AppContainer';
export { useAppContainer, AppRegistry, BackendIntegration } from './AppContainer';

// App Registry System
export { default as AppRegistryManager } from '../apps/AppRegistry';
